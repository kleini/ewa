#:kivy 1.0.9
#:import Gauge gauge

<Display>:
    spacing: 0
    orientation: 'vertical'
    gauge: gauge
    slider: slider
    Gauge:
        id: gauge
        size_hint: 1, .9
    Slider:
        id: slider
        size_hint: 1, .1
        on_value: root.set_gauge(value=self.value)
