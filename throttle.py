import smbus


class Throttle:
    def __init__(self):
        self.smbus = SMBus()


    def write:
        smbus.write_i2c_block(address, reg, data)


    def read:
        smbus.read_byte_data(address, reg)
